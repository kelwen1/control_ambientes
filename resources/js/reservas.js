/**
 * Funcionalidad específica para las páginas de reservas (create y edit)
 */
import { initReservaSelects } from './select-search';

// Inicializar cuando el DOM esté listo
document.addEventListener('DOMContentLoaded', function() {
    initReservaSelects();
});
